# Ignore sensitive files (Keep SAFETY FIRST)
.env
node_modules/

# Allowed: *.sqlite, excluded_users.json (User requested remove gitignore, so we track more files)
# *.sqlite
# excluded_users.json

# OS files
.DS_Store
*.log
